<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:Manga_App.ViewModels"
             x:Class="Manga_App.Pages.AuthorPage">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">
            <Label Text="Auteurs"
               FontSize="24"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

            <CollectionView ItemsSource="{Binding Authors}">

                <!--Dit toegevoegd zodat de data niet tegen elkaar blijft plakken-->
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="24"/>
                </CollectionView.ItemsLayout>
                
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="5"
                           Padding="10"
                           BorderColor="#ccc"
                           CornerRadius="10"
                           HasShadow="True">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding geboorteDatum, StringFormat='Geboortedatum: {0}'}" FontSize="14" />
                                <Label Text="{Binding Nationaliteit, StringFormat='Nationaliteit: {0}'}" FontSize="14" />
                                <Label Text="{Binding favoriteFood, StringFormat='Favoriet eten: {0}'}" FontSize="14" />
                                <Label Text="{Binding FavorieteSport, StringFormat='Favoriete sport: {0}'}" FontSize="14" />
                                <Label Text="{Binding description}" FontSize="12" LineBreakMode="WordWrap" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

    
</ContentPage>