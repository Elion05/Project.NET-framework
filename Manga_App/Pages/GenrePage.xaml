<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Manga_App.Pages.GenrePage"
             xmlns:viewmodel="clr-namespace:Manga_App.ViewModels">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="0">
            <Label Text="Genres"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,0,0,20" />
            <CollectionView ItemsSource="{Binding Genres}">
                
                <!--Dit toegevoegd zodat de data niet tegen elkaar blijft plakken-->
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="24"/>
                </CollectionView.ItemsLayout>
                
                
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="18"
                               BorderColor="#ccc"
                               CornerRadius="18"
                               HasShadow="True"
                               BackgroundColor="#F5F5F5">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}"
                                       FontSize="20"
                                       FontAttributes="Bold"
                                       TextColor="#222" />
                                <Label Text="{Binding genreBeschrijving}"
                                       FontSize="15"
                                       TextColor="#444"
                                       LineBreakMode="WordWrap" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>